[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA-style fallback for the CMS admin SPA routes
[[redirects]]
  from = "/admin/*"
  to = "/admin/index.html"
  status = 200
